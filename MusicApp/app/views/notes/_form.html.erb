<form action="<%= notes_url %>" method="POST">

  <input
    type="hidden"
    name="note[track_id]"
    value = "<%= track_id %>">

  <label>Note:<br>
    <textarea name="note[track_note]"><%= note.track_note %></textarea>
  </label>
  <br>

  <input type="submit" value="Tell the world!">
</form>
