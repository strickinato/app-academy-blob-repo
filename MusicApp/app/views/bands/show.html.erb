<h1><%= @band.name %></h1>

<ul>
  <% @band.albums.each do |album| %>
  <li><strong><%= link_to album.title, album_url(album.id) %></strong></li>
  <% end %>
</ul>

<ul>
  <li><%= link_to "Add Album", new_album_url %></li>
  <li><%= link_to "Change Band Name", edit_band_url(@band.id) %></li>
  <li><%= link_to "All Bands", bands_url %></li>
</ul>
<%= button_to "Delete this band", @band, :method => :delete %>
<p>
Editing
Destroying
Up one level (Track to Album, Album to Band, Band to bands index).
Down one level (Band to each Album, Album to each Track).
Also a link to build one more of each.
</p>
